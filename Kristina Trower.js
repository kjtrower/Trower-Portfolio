confirm("Are you intersted in learning about Kristina?");
var age=prompt("What is your age?");
console.log(age);
if (age >16)
{confirm("You are "+age+" and over the age of 16, congrats! Read all about her!");
}
else
{confirm("You are only "+age+", go play outside!");
}
